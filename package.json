{
  "name": "bottom-up-ddd-ts-sample",
  "version": "0.0.1",
  "author": "intercept6",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "backend:test": "yarn workspace backend run test",
    "ci:test": "yarn workspace backend run ci:test"
  }
}
